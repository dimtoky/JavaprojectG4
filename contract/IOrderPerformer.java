package contract;

/**
 * interface implemented in LorannController ( controller ) and used in GameFrame ( view )
 * 
 *
 */
public interface IOrderPerformer {
	void orderPerform(UserOrder userOrder);
}
